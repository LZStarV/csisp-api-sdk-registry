{
  "name": "@csisp-api/integrated-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "validate:proto": "tsx scripts/validate-proto.ts",
    "generate": "pnpm run validate:proto && tsx scripts/generate-grpc.ts",
    "build": "pnpm --filter @csisp-api/integrated-server run build && pnpm --filter @csisp-api/bff-integrated-server run build"
  },
  "devDependencies": {
    "grpc-tools": "^1.12.4",
    "ts-proto": "^1.176.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.9.0",
    "@grpc/proto-loader": "^0.7.0",
    "@nestjs/microservices": "^10.0.0",
    "long": "^5.3.2",
    "protobufjs": "^8.0.1",
    "rxjs": "^7.8.2"
  },
  "publishConfig": {
    "registry": "http://182.92.140.128:40087/"
  }
}
